[pytest]
markers =
    e2e: drives a head unit through an ARTEMIS host; needs ARTEMIS_URL
    carsim: feeds the launcher a simulated vehicle over adb + carsim.py; needs CARSIM_SERIAL
addopts = -p no:cacheprovider
